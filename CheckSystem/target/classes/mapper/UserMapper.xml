<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.UserMapper">
    <select id="selectAll" resultType="pojo.User">
        select * from user
    </select>
    <select id="find" resultType="pojo.User">
        select * from user where username = #{username} and password = #{password}
    </select>
    <select id="findNotYet" resultType="java.lang.String">
        select distinct u.username from user as u,check_in_out as c where u.username not in (select username from check_in_out)
    </select>
</mapper>
